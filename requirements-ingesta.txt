# Dependencias SOLO de la ingesta (Fase 2). El cron no necesita el bot.
#
# Versiones ACTUALES a proposito: publican wheels para Python 3.12, 3.13 y 3.14.
# Con versiones viejas, si Render usa 3.14 no encuentra binarios e intenta
# COMPILAR pandas/duckdb desde fuente -> el build tarda muchisimo y falla.

pandas==3.0.5
pytz==2024.2
duckdb==1.5.5           # motor temporal de carga (y destino local en dev)
gspread==6.2.1          # fuentes Google Sheets + registro maestro
google-auth==2.36.0
httpx==0.28.1           # fuentes api_rest / csv_url / sharepoint_excel
openpyxl==3.1.5         # fuente sharepoint_excel (lectura de .xlsx)

# Destino Neon / Postgres
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
